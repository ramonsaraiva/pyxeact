

class Physics:

    def __init__(self, scene):
        self.scene = scene
        scene.entities_subject.filter(
            lambda entity: 
        subscribe(self.verify_collisions)

    def verify_collisions(self, entity):

        pass
